<h2>Carts</h2>
<hr />
<h2></h2>

<div>
  
  <div>
    <p>cart:{{existe._id}}</p>
      {{#each existe.products}}
     
      
     

     <h3>producto:{{this.product.title}} </h3>
    <h3>precio:{{this.product.price}} </h3>
      <h3>cantidad:{{this.quantity}} </h3>
    
     <form action="/cart/{{../existe._id}}/product/{{this.product._id}}" method="post">
      <input type="hidden" name="_method" value="delete">
      <input type="submit" value="Delete">
  </form>
    
      {{/each}}

   
      
    <button onclick="finalizarCompra('{{existe._id}}')" >Finalizar Compra</button>

      <a href="/products">seguir comprando</a>
    {{!-- <li id="producto-{{_id}}">{{title}}</li></div> --}}
 
</div>
<a href="/">home</a>
  
   {{>menu}}
<script src="/js/carrito.js"></script>